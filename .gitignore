.venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
.streamlit/
# Regenerated by training; not needed by the deployed app
data/processed/*
!data/processed/.gitkeep
# The raw dataset (CC BY 4.0, see README attribution) and trained model ARE
# committed so the Streamlit Cloud deployment works out of the box.
CONTEXTO_PROYECTO.md
